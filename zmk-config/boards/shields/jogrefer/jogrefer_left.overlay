#include "jogrefer.dtsi" 

&kscan0 {
	col-gpios =
		<&gpio xxx GPIO_ACTIVE_HIGH>, // col1 in the schematic
		<&gpio xxx GPIO_ACTIVE_HIGH>, // col2 in the schematic
		<&gpio xxx GPIO_ACTIVE_HIGH>, // col3 in the schematic
		<&gpio xxx GPIO_ACTIVE_HIGH>, // col4 in the schematic
		<&gpio xxx GPIO_ACTIVE_HIGH>, // col5 in the schematic
		<&gpio xxx GPIO_ACTIVE_HIGH>; // col6 in the schematic
};